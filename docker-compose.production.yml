# docker-compose.production.yml

volumes:
  pg_data_production:
  static_volume:

# Всё отличие — заменяем build на image и указываем, какой образ использовать
services:
  db:
    image: postgres:13.10
    env_file: .env
    restart: unless-stopped
    volumes:
      - pg_data_production:/var/lib/postgresql/data
  backend:
    image: alexzzzz05/taski_backend # Качаем с Docker Hub
    env_file: .env
    restart: unless-stopped
    volumes:
      - static_volume:/backend_static
  frontend:
    image: alexzzzz05/taski_frontend  # Качаем с Docker Hub
    env_file: .env
    command: cp -r /app/build/. /frontend_static/
    volumes:
      - static_volume:/frontend_static
  gateway:
    image: alexzzzz05/taski_gateway  # Качаем с Docker Hub
    env_file: .env
    restart: unless-stopped
    volumes:
      - static_volume:/staticfiles/
    ports:
      - 8000:80
    depends_on:
      - backend